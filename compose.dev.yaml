services:
  atlas-dns:
    build:
      context: .
      args:
        VERSION: ${ATLAS_DNS_VERSION:-1.0.2-dev}
        COMMIT: ${ATLAS_DNS_COMMIT:-git-checkout}
        BUILT_AT: ${ATLAS_DNS_BUILT_AT:-local}
    image: atlas-dns-dev:${ATLAS_DNS_VERSION:-1.0.2-dev}
    pull_policy: never
